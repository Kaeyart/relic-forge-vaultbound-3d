# Patch 080C Repair — Loot filter global class registration

Rewrites the loot filter DB and system as parse-safe global classes so `RVLootFilterSystem` and `RVLootFilterDB` register correctly in Godot.
