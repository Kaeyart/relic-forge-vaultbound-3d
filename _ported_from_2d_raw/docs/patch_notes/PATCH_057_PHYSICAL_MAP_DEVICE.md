# Patch 057 — Physical Map Device + Map Loop Rework

Adds a physical Map Device hub station, a mouse/keyboard Map Device panel, stronger map item data, map run consumption, boss loot/map sustain improvements, and a dev `N` shortcut.

Test: walk to Map Device in hub, press E, select a map, run it, clear the map, kill the boss, loot, return hub, run again.
